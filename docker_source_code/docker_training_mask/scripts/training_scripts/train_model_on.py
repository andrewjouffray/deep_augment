import argparse
import os

parser.add_argument("-i", "--input", help = "The path to the training and testing data record files")
parser.add_argument("-m", "--model", help = "The path to the model to use")

args = parser.parse_args()




